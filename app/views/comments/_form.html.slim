= form_with( model: [place, comment], url: place_comments_path(@place), method: :post ) do |f|
  div id = 'error_explanation'
  = f.text_field :content
  = f.submit "コメントする", class: "btn btn-success comment-submit ml-3"
